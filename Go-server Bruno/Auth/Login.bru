meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{api}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "arashn2y@gmail.com",
    "password": "Arash2026!"
  }
}

script:post-response {
  let data = res.body;
  bru.setEnvVar("token", data.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
